Problem: Speculative Files Sandbox `/speculate` command (Phase 1) was not integrated in the terminal CLI and AetherCore pipeline.
Solution:
1. Exposed the modified overlay relative paths in `SpeculativeSandbox` via a public getter `writtenRelativePaths`.
2. Implemented the new `/speculate` prompt command inside `lib/cli/commands/commands/speculate_command.dart`.
3. Registered the speculate command loader in `lib/cli/commands/command_registry.dart`.
4. Wired the sandboxing lifecycle into the `_processInputEvent` loop of `AetherCore` in `lib/core/infrastructure/heartbeat/aether_core.dart`.
5. Developed a highly precise dynamic programming LCS-based git-style terminal diff generator (`_generateDiff`) and chunk-based formatter (`_printDiffLines`) supporting unified context in `AetherCore`.
6. Prompted the user via the interactive TUI dialog `inputAdapter.askQuestion` to Commit or Discard speculative changes cleanly.

Problem: LSP Compiler Intelligence (Dnyan Kosh) was locked to Dart/Flutter, lacked polyglot support for Python/JS/TS, and did not support semantic hover queries over real LSP channels.
Solution:
1. Expanded `LspClient` to implement semantic hover lookup via `getHover` querying the standard `textDocument/hover` method.
2. Built a robust `_parseHoverResponse` parser supporting all standard string, Map, and List-based markdown LSP response frames.
3. Created an auto-detection client registry `_activeClients` and `_failedLanguages` mapping file extensions (.dart, .py, .js, .ts, etc.) to respective standard language server executables (pyright-langserver/pylsp and typescript-language-server).
4. Wired the new polyglot client routing `_getOrConnectLspForFile` into `goto_definition`, `find_references`, and `hover` actions.
5. Upgraded `_findSymbolHover` fallback scanner to cleanly parse Python `#` comments.
6. Updated `dispose` to cleanly terminate and flush all concurrent active background language server processes, preventing resource leaks.

Problem: Pure Dart CLI environment lacked robust conversation session persistence (which previously crashed due to Flutter-specific method channels) and missed interactive rollback/undo capabilities in the command line.
Solution:
1. Upgraded `SessionManager` in `lib/core/infrastructure/services/session_manager.dart` to support a dynamic `customBasePath` constructor parameter, successfully bypassing Flutter `path_provider` dependencies in non-GUI shell instances.
2. Integrated `SessionManager` under `apex_sandbox/.apex_sessions/` in `bin/kharwal_cli.dart` to auto-restore session context on boot and persist message/tool histories cleanly on final responses and exits.
3. Enhanced `/session` command in `lib/cli/commands/commands/session_command.dart` with switcher sub-commands (`list`, `load`, `delete`, `create`) allowing users to swap, clear, and review conversation contexts instantly.
4. Built a brand new `/undo` command in `lib/cli/commands/commands/undo_command.dart` to list and revert modifications in a hybrid local snapshot and active Git-based checkout workflow, fully validated by newly added unit tests.

Problem: Filesystem edits, tool writes, and chat logs were vulnerable to corruption if session processes were terminated, and system cached tokens/fences (e.g. `<memory-context>`) could leak directly into the user stream.
Solution:
1. Engineered `AtomicWriteEngine` in `lib/core/infrastructure/services/atomic_write_engine.dart` supporting asynchronous and synchronous atomic file swaps via timestamped temp files.
2. Refactored file writes in `FileWriteTool`, surgical replacements in `FileEditTool`, backups in `RollbackHelper`, and session index/history updates in `SessionManager` to execute atomically, guaranteeing 100% data integrity.
3. Implemented a stateful sliding-window lookahead filter `StreamingContextScrubber` in `lib/core/infrastructure/services/streaming_context_scrubber.dart` to identify and remove fencing tags (even when split across network chunks).
4. Wired the stateful context scrubbers directly into `kharwal_cli.dart` events to completely prevent tag leakages inside the console, fully covered and validated by newly added unit tests.

Problem: The sandboxed shell execution system executed commands stateless, lacking environment variable (`export`), function, alias, and active working directory tracking across isolated process invocations, and missed robust cross-platform validation.
Solution:
1. Created `ApexStatefulShell` in `lib/core/infrastructure/services/apex_stateful_shell.dart` to persistently bootstrap and snapshot env variables, aliases, and working directory state in regular interactive shells (`bash`/`sh`).
2. Integrated `ApexStatefulShell` into `SpectralOps` constructor asynchronously to prevent execution block, and synchronized working directory changes back to the sandbox boundary.
3. Decoupled UI-specific Flutter packages like `path_provider` from the core storage logic, initializing them at the top-level UI layer (`lib/main.dart`) to ensure error-free compiling for both pure CLI executables and Flutter apps.
4. Resolved a mock callModel history checking issue in the phase3 hardening test suite (`test/phase3_hardening_test.dart`) where injected workspace context system messages masked the active prompt roles, and expanded testing delay bounds to successfully absorb process spawning latency.
5. Resolved a missing compiler import error (`Error: Method not found: 'AetherHistoryCompactor'`) inside `bin/kharwal_cli.dart` by importing the history compactor module, and cleaned up unused dependencies.
6. Removed all competitor naming and brand references from comments, models list, and CLI banners, replacing them with custom brand terminology.
7. Fixed a critical Plan Mode crash (Waterfall failure) where entering plan mode unconditionally escalated the runtime model to the Gemini-specific model name (`gemini-2.5-flash`) even on other active providers (like Groq, NVIDIA, Ollama, Custom), throwing Model Not Found HTTP exceptions. Implemented provider-aware model matching in `PlanModeCoordinator.getRuntimeModel` to resolve correct reasoning models dynamically for all active providers.
8. Engineered a "Smart Efficiency & Free-First" model selection policy in Plan Mode. If the active model name indicates an efficient or free tier (contains `free`, `8b`, `9b`, `gemma-2`), the coordinator respects the user's intent: on OpenRouter it routes to high-quality free models (like `gemma-2-9b-it:free`), and on other providers it keeps the exact chosen model. Replaced heavy, slow, and expensive models (like 405B on NVIDIA) with ultra-fast, cheap, and highly capable alternatives (like Llama 3.1 8B/70B on NVIDIA, and Llama 3.1 8B on Groq), boosting response speed 100x and preventing developer credit exhaustion.



